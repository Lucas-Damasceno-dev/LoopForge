# RBAC na UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Multi-usuário real na SPA do LoopForge: login com API key, roles viewer<runner<admin aplicadas na UI (views e ações) e gap de segurança fechado (CRUD de pipelines admin-only).

**Architecture:** Backend expõe `GET /api/v1/auth/me` (principal + roles) e fecha a matriz RBAC p/ pipelines; frontend ganha auth store (zustand), login validado no ApiKeyGate, e gating de views/ações por role via helper `can(role)`. Sem react-router — gating na ActivityRail/SidebarHost/componentes.

**Tech Stack:** Python FastAPI (engine `agentes/LoopForge`), React 19 + zustand + Vite (SPA `web/loopforge-ade/frontend`), Vitest + Playwright.

## Global Constraints

- Idioma: docs/comentários em PT, identificadores em EN.
- Backend: ruff `--select E,F,W,I,N,UP,SIM` (line-length 120, ignore E501/SIM117/E402/F401), mypy `src/lf`, pytest `tests/` (coverage ≥75%).
- Frontend: `npm run lint && npm run test && npm run build` em `web/loopforge-ade/frontend`.
- Hierarquia de roles (fonte única backend `auth.py:25`): `viewer < runner < admin` — `can(role)` no frontend espelha `Principal.has_role` (nível máximo ≥ exigido).
- SPA: navegação por `viewStore` (sem react-router); key em localStorage `lf_api_key` (`api.ts:48`); 401 → retry queue (`api.ts:81-130`).
- BC: auth desativada (sem `LF_API_REQUIRE_AUTH`/keys) → principal `anonymous/admin` — UI continua assumindo admin (demo/modo local).
- E2E: `window.__lfTest` dev hook (`App.tsx:112-120`), helper `dismissApiKeyGate` em `tests/helpers.ts`.

---

### Task 1: Backend — `GET /api/v1/auth/me` (endpoint + dependency)

**Files:**
- Modify: `agentes/LoopForge/src/lf/api/auth.py`
- Modify: `agentes/LoopForge/src/lf/api/schemas.py`
- Modify: `agentes/LoopForge/src/lf/api/app.py` (adicionar rota perto de `create_run_v1`, ~:380)
- Test: `agentes/LoopForge/tests/test_auth_rbac.py`

**Interfaces:**
- Consumes: `Principal` (auth.py:28-38), `APISettings` (config.py:57), `_resolve_principal` (auth.py:134), `_auth_enabled` (auth.py:127)
- Produces: `get_current_principal(...) -> Principal` (dependency FastAPI, auth.py); `AuthMeResponse{name: str, roles: list[str]}` (schemas.py); rota `GET /api/v1/auth/me`

- [ ] **Step 1: Escrever os testes que falham**

Adicionar em `tests/test_auth_rbac.py`:

```python
@pytest.mark.asyncio
async def test_auth_me_retorna_principal(tmp_path):
    """/auth/me devolve name+roles do principal da key."""
    _write_ade(tmp_path, _ade_keys())
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/v1/auth/me", headers={"X-API-Key": RUNNER_KEY})
        assert r.status_code == 200
        body = r.json()
        assert body["name"] == "runner-svc"
        assert body["roles"] == ["runner"]


@pytest.mark.asyncio
async def test_auth_me_env_key_admin(tmp_path):
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/v1/auth/me", headers={"X-API-Key": ENV_KEY})
        assert r.status_code == 200
        assert r.json()["roles"] == ["admin"]


@pytest.mark.asyncio
async def test_auth_me_sem_key_401(tmp_path):
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        assert (await ac.get("/api/v1/auth/me")).status_code == 401


@pytest.mark.asyncio
async def test_auth_me_auth_off_anonymous_admin(tmp_path, monkeypatch):
    """Auth desativada → principal anônimo admin (BC: UI assume admin)."""
    monkeypatch.delenv("LF_API_API_KEY", raising=False)
    monkeypatch.delenv("LF_API_REQUIRE_AUTH", raising=False)
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/v1/auth/me")
        assert r.status_code == 200
        assert r.json() == {"name": "anonymous", "roles": ["admin"]}
```

- [ ] **Step 2: Rodar p/ ver falhar**

Run: `pytest tests/test_auth_rbac.py -x -k auth_me`
Expected: FAIL (rota inexistente → 404/405)

- [ ] **Step 3: Implementar dependency + schema + rota**

`auth.py` — extrair resolução de principal (reuso entre verify_authentication e a nova dependency). Adicionar após `_resolve_principal` (:138):

```python
def _principal_from_request(
    request: Request,
    credentials: HTTPBasicCredentials | None,
    api_key_header: str | None,
    settings: APISettings,
) -> Principal | None:
    """Resolve o principal de headers (X-API-Key → Basic), ou None."""
    principal = _resolve_principal(api_key_header, settings)
    if principal is None and credentials is not None:
        principal = _resolve_principal(credentials.username, settings)
        if principal is None:
            principal = _resolve_principal(credentials.password, settings)
    return principal


def get_current_principal(
    request: Request,
    credentials: HTTPBasicCredentials | None = Security(security_basic),
    api_key_header: str | None = Security(security_api_key),
) -> Principal:
    """Dependency: principal autenticado (401 se auth ativa e inválido).

    Auth desativada → principal anônimo admin (BC: a UI assume admin).
    """
    settings = APISettings()
    if not _auth_enabled(settings):
        return Principal(name="anonymous", roles=("admin",))
    principal = _principal_from_request(request, credentials, api_key_header, settings)
    if principal is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Credenciais inválidas ou ausentes.",
            headers={"WWW-Authenticate": "Basic"},
        )
    return principal
```

Refatorar `verify_authentication` (:146-182) para usar `_principal_from_request` (mesmo comportamento, sem duplicação):

```python
def verify_authentication(
    request: Request,
    credentials: HTTPBasicCredentials | None = Security(security_basic),
    api_key_header: str | None = Security(security_api_key),
) -> bool:
    """Verifica autenticação (Basic ou X-API-Key) e aplica RBAC por método+path."""
    settings = APISettings()
    if not _auth_enabled(settings):
        return True
    principal = _principal_from_request(request, credentials, api_key_header, settings)
    if principal is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Credenciais inválidas ou ausentes.",
            headers={"WWW-Authenticate": "Basic"},
        )
    required = _required_role(request.method, request.url.path)
    if not principal.has_role(required):
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Permissão insuficiente: role '{required}' exigida para "
                f"{request.method} {request.url.path} (principal '{principal.name}')."
            ),
        )
    return True
```

`schemas.py` — adicionar no fim:

```python
class AuthMeResponse(BaseModel):
    """GET /api/v1/auth/me — identidade do principal autenticado."""

    name: str
    roles: list[str]
```

`app.py` — rota (adicionar após `create_run_v1`, ~:382; importar `get_current_principal` de `lf.api.auth`):

```python
    @app.get(
        "/api/v1/auth/me",
        response_model=AuthMeResponse,
        tags=["Auth"],
    )
    async def auth_me(principal: Principal = Depends(get_current_principal)) -> AuthMeResponse:
        """Identidade do principal (name + roles) — base do login da SPA."""
        return AuthMeResponse(name=principal.name, roles=list(principal.roles))
```

Imports necessários no `app.py`: `AuthMeResponse` de `lf.api.schemas`; `get_current_principal`, `Principal` de `lf.api.auth`; `Depends` de `fastapi`.

- [ ] **Step 4: Rodar testes p/ passar**

Run: `pytest tests/test_auth_rbac.py`
Expected: PASS (todos os testes do arquivo)

- [ ] **Step 5: Commit**

```bash
git add src/lf/api/auth.py src/lf/api/schemas.py src/lf/api/app.py tests/test_auth_rbac.py
git commit -m "feat(api): GET /api/v1/auth/me expõe principal (name+roles)"
```

---

### Task 2: Backend — matriz RBAC: pipelines CRUD admin-only

**Files:**
- Modify: `agentes/LoopForge/src/lf/api/auth.py` (`_required_role`, :66-102)
- Test: `agentes/LoopForge/tests/test_auth_rbac.py`

**Interfaces:**
- Consumes: `_required_role(method, path) -> str` (auth.py:66)
- Produces: regra de matriz: `POST/PUT/DELETE /api/v1/pipelines*` → `admin`; `GET` permanece `viewer`

- [ ] **Step 1: Testes que falham**

Adicionar a `test_matriz_roles` (:148) e um teste de integração novo:

```python
    # S3 RBAC: pipelines CRUD é admin-only (GET continua viewer)
    assert _required_role("POST", "/api/v1/pipelines") == "admin"
    assert _required_role("PUT", "/api/v1/pipelines/abc") == "admin"
    assert _required_role("DELETE", "/api/v1/pipelines/abc") == "admin"
    assert _required_role("GET", "/api/v1/pipelines") == "viewer"


@pytest.mark.asyncio
async def test_runner_403_pipelines_admin_201(tmp_path):
    """Runner não cria pipeline (403); admin cria (201)."""
    _write_ade(tmp_path, _ade_keys())
    app = create_app()
    body = {"name": "p-rbac", "description": "", "nodes": [], "edges": []}
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        assert (
            await ac.post("/api/v1/pipelines", json=body, headers={"X-API-Key": RUNNER_KEY})
        ).status_code == 403
        assert (
            await ac.post("/api/v1/pipelines", json=body, headers={"X-API-Key": ENV_KEY})
        ).status_code == 201
```

- [ ] **Step 2: Rodar p/ ver falhar**

Run: `pytest tests/test_auth_rbac.py -x -k "matriz or pipelines"`
Expected: FAIL (`POST /api/v1/pipelines` → runner hoje, 201 no lugar de 403)

- [ ] **Step 3: Implementar regra na matriz**

Em `_required_role` (:66), adicionar no bloco ADMIN, antes das regras de runner (:83):

```python
    # ADMIN
    ...
    if p.startswith("/api/v1/pipelines") and m in ("POST", "PUT", "DELETE"):
        return "admin"
```

- [ ] **Step 4: Rodar testes p/ passar**

Run: `pytest tests/test_auth_rbac.py && pytest tests/ -q` (suíte completa, 107 arquivos)
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/lf/api/auth.py tests/test_auth_rbac.py
git commit -m "feat(api): CRUD de pipelines exige role admin (S3 RBAC)"
```

---

### Task 3: Frontend — `AuthMe` type + `getAuthMe` + auth store

**Files:**
- Modify: `web/loopforge-ade/frontend/src/shared/lib/types.ts`
- Modify: `web/loopforge-ade/frontend/src/shared/lib/api.ts`
- Create: `web/loopforge-ade/frontend/src/stores/authStore.ts`
- Test: `web/loopforge-ade/frontend/src/stores/__tests__/authStore.test.ts` (criar dir se não existir; seguir padrão de `stores/` existente)

**Interfaces:**
- Consumes: `apiFetch<T>` (api.ts:109), `getApiKey`/`setApiKey` (api.ts:50-60), `onUnauthorized`/`retryUnauthorizedRequests`/`rejectPendingUnauthorized` (api.ts:84-107)
- Produces: `interface AuthMe {name: string; roles: string[]}` (types.ts); `getAuthMe() -> Promise<AuthMe>` (api.ts); `useAuthStore` com `{principal, login(key), logout(), can(required), setPrincipal}`

- [ ] **Step 1: Testes que falham**

`src/stores/__tests__/authStore.test.ts`:

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { useAuthStore } from '../authStore'
import { getAuthMe, getApiKey, setApiKey } from '../../shared/lib/api'

vi.mock('../../shared/lib/api', () => ({
  getAuthMe: vi.fn(),
  getApiKey: vi.fn(() => undefined),
  setApiKey: vi.fn(),
  retryUnauthorizedRequests: vi.fn(),
  rejectPendingUnauthorized: vi.fn(),
}))

const mockedGetAuthMe = vi.mocked(getAuthMe)

describe('authStore', () => {
  beforeEach(() => {
    localStorage.clear()
    useAuthStore.setState({ principal: null })
  })

  it('login salva key, busca principal e persiste', async () => {
    mockedGetAuthMe.mockResolvedValue({ name: 'runner-svc', roles: ['runner'] })
    await useAuthStore.getState().login('runner-key-456')
    expect(setApiKey).toHaveBeenCalledWith('runner-key-456')
    expect(useAuthStore.getState().principal).toEqual({ name: 'runner-svc', roles: ['runner'] })
    expect(localStorage.getItem('lf_auth_principal')).toContain('runner-svc')
  })

  it('login com key inválida rejeita e não seta principal', async () => {
    mockedGetAuthMe.mockRejectedValue(new Error('API 401'))
    await expect(useAuthStore.getState().login('bad')).rejects.toThrow()
    expect(useAuthStore.getState().principal).toBeNull()
  })

  it('logout limpa principal e key', () => {
    localStorage.setItem('lf_auth_principal', JSON.stringify({ name: 'a', roles: ['admin'] }))
    useAuthStore.setState({ principal: { name: 'a', roles: ['admin'] } })
    useAuthStore.getState().logout()
    expect(useAuthStore.getState().principal).toBeNull()
  })

  it('can(): sem principal → true (BC admin); hierarquia viewer<runner<admin', () => {
    expect(useAuthStore.getState().can('admin')).toBe(true)
    useAuthStore.setState({ principal: { name: 'v', roles: ['viewer'] } })
    expect(useAuthStore.getState().can('viewer')).toBe(true)
    expect(useAuthStore.getState().can('runner')).toBe(false)
    useAuthStore.setState({ principal: { name: 'r', roles: ['runner'] } })
    expect(useAuthStore.getState().can('runner')).toBe(true)
    expect(useAuthStore.getState().can('admin')).toBe(false)
    useAuthStore.setState({ principal: { name: 'a', roles: ['admin'] } })
    expect(useAuthStore.getState().can('admin')).toBe(true)
  })
})
```

- [ ] **Step 2: Rodar p/ ver falhar**

Run: `npx vitest run src/stores/__tests__/authStore.test.ts`
Expected: FAIL (módulo authStore não existe)

- [ ] **Step 3: Implementar**

`types.ts` — adicionar perto de `Run` (:78):

```ts
/** GET /api/v1/auth/me — identidade do principal (name + roles). */
export interface AuthMe {
  name: string
  roles: string[]
}
```

`api.ts` — após `getHealth` (:356):

```ts
// ─── Auth (RBAC) — GET /api/v1/auth/me ─────────────────────────────────────
// Identidade do principal (name + roles) para o login da SPA. 401 → retry
// queue padrão (o gate reabre); auth desativada → anonymous/admin.
export const getAuthMe = () => apiFetch<AuthMe>('/auth/me')
```

E adicionar `AuthMe` ao bloco de imports de types (api.ts:1-39).

`src/stores/authStore.ts`:

```ts
import { create } from 'zustand'
import { getAuthMe, getApiKey, setApiKey, rejectPendingUnauthorized, retryUnauthorizedRequests } from '../shared/lib/api'
import type { AuthMe } from '../shared/lib/types'

// ─── Auth store (RBAC) ──────────────────────────────────────────────────────
// Principal autenticado (GET /auth/me) + key em localStorage 'lf_api_key'
// (já usada pelo api.ts). can(role) espelha Principal.has_role do backend
// (nível máximo ≥ exigido). Sem principal → true (BC: auth off/demo assume
// admin). Principal persistido em 'lf_auth_principal' (login não refaz
// /auth/me a cada reload).

const PRINCIPAL_STORAGE = 'lf_auth_principal'
const ROLE_LEVEL: Record<string, number> = { viewer: 0, runner: 1, admin: 2 }

function loadPrincipal(): AuthMe | null {
  try {
    const raw = localStorage.getItem(PRINCIPAL_STORAGE)
    return raw ? (JSON.parse(raw) as AuthMe) : null
  } catch {
    return null
  }
}

interface AuthState {
  principal: AuthMe | null
  login: (key: string) => Promise<void>
  logout: () => void
  setPrincipal: (p: AuthMe | null) => void
  can: (required: string) => boolean
}

export const useAuthStore = create<AuthState>((set, get) => ({
  principal: loadPrincipal(),

  login: async (key: string) => {
    setApiKey(key)
    const principal = await getAuthMe()
    try {
      localStorage.setItem(PRINCIPAL_STORAGE, JSON.stringify(principal))
    } catch {
      /* storage indisponível (teste/privacy) */
    }
    set({ principal })
    retryUnauthorizedRequests() // reexecuta pendentes com a nova key
  },

  logout: () => {
    try {
      localStorage.removeItem(PRINCIPAL_STORAGE)
      localStorage.removeItem('lf_api_key')
    } catch {
      /* storage indisponível */
    }
    set({ principal: null })
    rejectPendingUnauthorized()
  },

  setPrincipal: (p) => set({ principal: p }),

  can: (required: string) => {
    const p = get().principal
    if (!p) return true // auth off / demo → BC admin
    const maxLevel = Math.max(...p.roles.map((r) => ROLE_LEVEL[r] ?? 0))
    return maxLevel >= (ROLE_LEVEL[required] ?? 0)
  },
}))
```

Nota: `getApiKey` importado p/ clareza de intenção — se lint acusar unused, remova-o do import (logout usa `localStorage.removeItem` direto, padrão do `api.ts:59`).

- [ ] **Step 4: Rodar testes p/ passar**

Run: `npx vitest run src/stores/__tests__/authStore.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/shared/lib/types.ts src/shared/lib/api.ts src/stores/authStore.ts src/stores/__tests__/authStore.test.ts
git commit -m "feat(ui): auth store (login/logout/can) + getAuthMe"
```

---

### Task 4: Frontend — ApiKeyGate valida login + chip de usuário/logout na Topbar

**Files:**
- Modify: `web/loopforge-ade/frontend/src/features/auth/ApiKeyGate.tsx`
- Modify: `web/loopforge-ade/frontend/src/app/App.tsx` (região `right` da Topbar, :337-355)
- Test: `web/loopforge-ade/frontend/src/features/auth/__tests__/ApiKeyGate.test.tsx` (verificar dir `__tests__` existente em features/auth; criar se necessário)

**Interfaces:**
- Consumes: `useAuthStore.login/logout/principal` (Task 3); `getApiKey` (api.ts:50)
- Produces: gate com submit → `login(key)` (valida via /auth/me, erro inline em 401/403); chip na Topbar com `principal.name` + roles + botão logout (visível só com principal real, não `anonymous`)

- [ ] **Step 1: Testes que falham**

`src/features/auth/__tests__/ApiKeyGate.test.tsx` (mockar `../../stores/authStore` e `../../shared/lib/api`; renderizar `<ApiKeyGate />`):

```tsx
import { describe, expect, it, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import { ApiKeyGate } from '../ApiKeyGate'
import { useAuthStore } from '../../../stores/authStore'

vi.mock('../../../shared/lib/api', () => ({
  getApiKey: vi.fn(() => undefined),
  setApiKey: vi.fn(),
  onUnauthorized: vi.fn(() => () => {}),
  retryUnauthorizedRequests: vi.fn(),
  rejectPendingUnauthorized: vi.fn(),
}))

vi.mock('../../../stores/authStore', () => {
  const actual = vi.importActual('../../../stores/authStore')
  return { ...actual }
})

describe('ApiKeyGate', () => {
  beforeEach(() => {
    localStorage.clear()
    useAuthStore.setState({ principal: null })
  })

  it('submit com key inválida mostra erro inline e mantém gate aberto', async () => {
    render(<ApiKeyGate />)
    fireEvent.change(screen.getByLabelText('API key'), { target: { value: 'bad-key' } })
    fireEvent.click(screen.getByText('Save & retry'))
    await waitFor(() => {
      expect(screen.getByRole('alert')).toBeTruthy()
    })
    expect(useAuthStore.getState().principal).toBeNull()
  })

  it('submit com key válida fecha o gate e seta principal', async () => {
    useAuthStore.setState({
      login: vi.fn(async () => {
        useAuthStore.setState({ principal: { name: 'runner-svc', roles: ['runner'] } })
      }),
    } as never)
    render(<ApiKeyGate />)
    fireEvent.change(screen.getByLabelText('API key'), { target: { value: 'runner-key-456' } })
    fireEvent.click(screen.getByText('Save & retry'))
    await waitFor(() => {
      expect(useAuthStore.getState().principal?.name).toBe('runner-svc')
    })
  })
})
```

- [ ] **Step 2: Rodar p/ ver falhar**

Run: `npx vitest run src/features/auth/__tests__/ApiKeyGate.test.tsx`
Expected: FAIL (comportamento atual: salva key sem validar, fecha sempre)

- [ ] **Step 3: Implementar**

`ApiKeyGate.tsx` — substituir o handler `submit` (:28-40) por versão que valida via login:

```tsx
  const submit = async (e: FormEvent) => {
    e.preventDefault()
    const trimmed = key.trim()
    if (!trimmed) {
      setError('Informe a API key')
      return
    }
    setError(null)
    try {
      await useAuthStore.getState().login(trimmed)
      setOpen(false)
      setKey('')
    } catch (err) {
      setError(err instanceof Error ? err.message : 'API key inválida')
    }
  }
```

Atualizar imports: remover `getApiKey`, `setApiKey`, `retryUnauthorizedRequests`, `rejectPendingUnauthorized` de `../../shared/lib/api` (login/logout agora no store); adicionar `import { useAuthStore } from '../../stores/authStore'`. Manter `onUnauthorized` (gate reabre em 401 de sessão). Mantém o botão "Continue without backend" (`dismiss` → `rejectPendingUnauthorized` — trocar por `useAuthStore.getState().logout()` para consistência; logout é seguro mesmo sem key salva).

`App.tsx` — no componente `App` (:55), adicionar:

```tsx
  const principal = useAuthStore((s) => s.principal)
  const logout = useAuthStore((s) => s.logout)
```

Na região `right` da Topbar (:337-355), antes dos botões Inspector/Focus:

```tsx
                {principal && principal.name !== 'anonymous' && (
                  <span className="inline-flex items-center gap-1.5 rounded-full border border-[var(--border)] bg-[var(--bg-elev)] px-2 py-0.5">
                    <span className="max-w-28 truncate text-xs text-[var(--text)]" title={principal.name}>
                      {principal.name}
                    </span>
                    <span className="text-(--text-2xs) uppercase tracking-wide text-[var(--text-dim)]">
                      {principal.roles.join('/')}
                    </span>
                    <button
                      type="button"
                      onClick={logout}
                      title="Logout"
                      aria-label="Logout"
                      className="flex h-4 w-4 items-center justify-center rounded text-[var(--text-dim)] hover:bg-[var(--bg-elev-2)] hover:text-[var(--text)]"
                    >
                      ✕
                    </button>
                  </span>
                )}
```

Import `useAuthStore` em App.tsx (:24-46).

- [ ] **Step 4: Rodar testes p/ passar**

Run: `npx vitest run src/features/auth/__tests__/ApiKeyGate.test.tsx && npm run lint`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/features/auth/ApiKeyGate.tsx src/app/App.tsx src/features/auth/__tests__/ApiKeyGate.test.tsx
git commit -m "feat(ui): login validado no gate + chip de usuário/logout na topbar"
```

---

### Task 5: Frontend — views role-aware (rail + sidebar + palette)

**Files:**
- Modify: `web/loopforge-ade/frontend/src/shared/lib/views.ts`
- Modify: `web/loopforge-ade/frontend/src/shared/ui/ActivityRail.tsx`
- Modify: `web/loopforge-ade/frontend/src/shared/ui/SidebarHost.tsx`
- Modify: `web/loopforge-ade/frontend/src/shared/lib/commands.ts` (+ `CommandPalette` se necessário)
- Test: `web/loopforge-ade/frontend/src/shared/ui/__tests__/ActivityRail.test.tsx` (verificar dir existente)

**Interfaces:**
- Consumes: `useAuthStore.can` (Task 3), `WORKSPACE_GROUPS`/`ViewKey` (views.ts:9-63)
- Produces: `VIEW_ROLE: Partial<Record<ViewKey, string>>` (views.ts); rail/sidebar filtram views por role; `Command.role?: string` filtrado na palette

- [ ] **Step 1: Testes que falham**

`src/shared/ui/__tests__/ActivityRail.test.tsx`:

```tsx
import { describe, expect, it, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { ActivityRail } from '../ActivityRail'
import { useAuthStore } from '../../../stores/authStore'

vi.mock('../../../stores/authStore', () => ({
  useAuthStore: vi.fn((selector) => selector(useAuthStoreState)),
}))

let useAuthStoreState: { can: (r: string) => boolean }

describe('ActivityRail role filtering', () => {
  beforeEach(() => {
    useAuthStoreState = { can: () => true }
  })

  it('admin (can=true) vê todas as views, incluindo mcp e settings', () => {
    render(<ActivityRail active="runs" onSelect={vi.fn()} />)
    expect(screen.getByLabelText('MCP playground')).toBeTruthy()
    expect(screen.getByLabelText('Settings')).toBeTruthy()
  })

  it('viewer (can=false p/ admin) não vê mcp nem settings', () => {
    useAuthStoreState = { can: (r) => r !== 'admin' }
    render(<ActivityRail active="runs" onSelect={vi.fn()} />)
    expect(screen.queryByLabelText('MCP playground')).toBeNull()
    expect(screen.queryByLabelText('Settings')).toBeNull()
    expect(screen.getByLabelText('Runs')).toBeTruthy()
  })
})
```

Ajuste o mock para o padrão real do zustand (`useAuthStore((s) => s.can)`) — ver arquivo de teste existente mais próximo (ex.: `src/stores/__tests__/viewStore.test.ts`) para o padrão de mock de store com selector; o mock acima é a intenção, adapte ao padrão do repo.

- [ ] **Step 2: Rodar p/ ver falhar**

Run: `npx vitest run src/shared/ui/__tests__/ActivityRail.test.tsx`
Expected: FAIL (rail não filtra — mcp/settings visíveis p/ viewer)

- [ ] **Step 3: Implementar**

`views.ts` — adicionar após `WORKSPACE_GROUPS` (:63):

```ts
/** Role mínima p/ ver cada view (RBAC). Ausente = 'viewer' (default). */
export const VIEW_ROLE: Partial<Record<ViewKey, string>> = {
  mcp: 'admin',
  settings: 'admin',
}
```

`ActivityRail.tsx` — filtrar grupos:

```tsx
import { useAuthStore } from '../../stores/authStore'
import { VIEW_ROLE, VIEWS_META, WORKSPACE_GROUPS } from '../lib/views'
```

No corpo do componente:

```tsx
  const can = useAuthStore((s) => s.can)
```

E no map dos grupos (:24):

```tsx
      {WORKSPACE_GROUPS.map(({ group, views }, gi) => {
        const allowed = views.filter((key) => can(VIEW_ROLE[key] ?? 'viewer'))
        if (allowed.length === 0) return null
        return (
          <div key={group}>
            {gi > 0 && <div className="mx-1 my-1 h-px bg-[var(--border)]" aria-hidden="true" />}
            {allowed.map((key) => { /* corpo atual do botão, inalterado */ })}
          </div>
        )
      })}
```

(Manter JSX interno do botão idêntico ao atual.)

`SidebarHost.tsx` — guard no topo do componente (após hooks, antes do `if (!active) return null`):

```tsx
  const can = useAuthStore((s) => s.can)
  ...
  if (!active) return null
  if (!can(VIEW_ROLE[active] ?? 'viewer')) return null
```

(Importar `VIEW_ROLE` de `../lib/views` e `useAuthStore` de `../../stores/authStore`.)

`commands.ts` — adicionar role opcional:

```ts
export interface Command {
  id: string
  title: string
  kbd?: string
  keywords: string[]
  /** Role mínima (RBAC). Ausente = qualquer principal. */
  role?: string
  run: (ctx: PaletteCtx) => void
}
```

Marcar: `new-run` → `role: 'runner'`; `budget-override` → `role: 'admin'`. No `CommandPalette.tsx`, filtrar com `useAuthStore((s) => s.can)`: `const visible = filtered.filter((c) => !c.role || can(c.role))` (ler o arquivo para achar o ponto exato de render da lista e aplicar o filtro no `filterCommands` já usado).

- [ ] **Step 4: Rodar testes p/ passar**

Run: `npx vitest run src/shared/ui/__tests__/ActivityRail.test.tsx && npm run lint && npm run build`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/shared/lib/views.ts src/shared/ui/ActivityRail.tsx src/shared/ui/SidebarHost.tsx src/shared/lib/commands.ts src/shared/ui/__tests__/ActivityRail.test.tsx
git commit -m "feat(ui): views e commands filtrados por role (RBAC)"
```

---

### Task 6: Frontend — ações role-aware (criar run, cancel/resume, pipelines editor, budget override)

**Files:**
- Modify: `web/loopforge-ade/frontend/src/features/runs/NewRunForm.tsx`
- Modify: `web/loopforge-ade/frontend/src/features/runs/RunsWorkspace.tsx`
- Modify: `web/loopforge-ade/frontend/src/features/pipelines/PipelinesPanel.tsx`
- Modify: `web/loopforge-ade/frontend/src/app/App.tsx` (barra do editor :247-291 + BudgetPill onOverride :304-311)
- Test: `web/loopforge-ade/frontend/src/features/runs/__tests__/NewRunForm.test.tsx` (atualizar existente) + `web/loopforge-ade/frontend/src/features/runs/__tests__/RunsWorkspace.test.tsx` (verificar existência)

**Interfaces:**
- Consumes: `useAuthStore.can` (Task 3)
- Produces: `can('runner')` gateia criação/cancel/resume; `can('admin')` gateia editor de pipelines (Save/Validate/Edit/Live/Close + New/Edit/Delete no PipelinesPanel) e override de budget

- [ ] **Step 1: Testes que falham**

`NewRunForm.test.tsx` — adicionar caso (mock do store de auth, padrão do arquivo existente):

```tsx
  it('viewer não vê o formulário (read-only notice)', async () => {
    vi.mocked(useAuthStoreState.can).mockReturnValue(false) // can('runner') false
    render(<NewRunForm onCreated={vi.fn()} />)
    expect(screen.queryByLabelText('Idea')).toBeNull()
    expect(screen.getByText(/read-only/i)).toBeTruthy()
  })
```

Adaptar ao mock real do arquivo: o NewRunForm chama `useAuthStore((s) => s.can)`; mockar o store (padrão do teste existente do NewRunForm, que mocka api + pipelines store).

`RunsWorkspace.test.tsx` — caso viewer: botões Resume/Cancel ausentes mesmo com run paused/running:

```tsx
  it('viewer não vê Resume/Cancel', () => {
    // estado: run paused no store + can('runner') false
    render(<RunsWorkspace />)
    expect(screen.queryByText('Resume')).toBeNull()
    expect(screen.queryByText('Cancel')).toBeNull()
  })
```

- [ ] **Step 2: Rodar p/ ver falhar**

Run: `npx vitest run src/features/runs/__tests__/NewRunForm.test.tsx src/features/runs/__tests__/RunsWorkspace.test.tsx`
Expected: FAIL (form e botões visíveis p/ viewer)

- [ ] **Step 3: Implementar**

`NewRunForm.tsx` — no componente:

```tsx
  const can = useAuthStore((s) => s.can)
```

E antes do `return` principal (:116):

```tsx
  if (!can('runner')) {
    return (
      <p className="px-0.5 text-xs text-[var(--text-dim)]">
        Read-only — role runner ou admin necessária para iniciar runs.
      </p>
    )
  }
```

`RunsWorkspace.tsx` — gatear Resume (:167-171) e Cancel (:174-178):

```tsx
            {canRun && activeRunPaused && ( /* botão Resume atual */ )}
            {canRun && runCancellable && ( /* botão Cancel atual */ )}
```

com `const canRun = useAuthStore((s) => s.can('runner'))` (selector estável — can não muda de referência; se o lint reclamar de selector retornando novo valor, usar `const can = useAuthStore((s) => s.can); const canRun = can('runner')`).

`PipelinesPanel.tsx` — `const canAdmin = useAuthStore((s) => s.can('admin'))` (ou can + chamada); gatear:
- botão "+ New pipeline" (:127): `{canAdmin && (<Button ...>)}`
- EmptyState action "Create pipeline" (:139): `{canAdmin && (<Button ...>)}`
- `onClick` do item da lista (:150): `onClick={() => { if (canAdmin) startEdit(p) }}` (viewer vê lista, sem edição)
- bloco Delete (:202-221): envolver com `canAdmin &&`
- botão Submit "Save" (:224): `disabled={saving || !canAdmin}` + `type="submit"` (ou ocultar p/ não-admin: `{canAdmin && (...)}` — preferir ocultar, consistente com New/Delete)
- botão "Edit in canvas" (:181-192): `{canAdmin && selectedId !== 'new' ? (...) : null}`

`App.tsx` — barra do editor (:247): `{editorOpen && canAdmin && ( ... )}`; `savePipeline`/`runValidate` já são disparados da barra (inacessível p/ não-admin); BudgetPill `onOverride` (:305-310):

```tsx
        <BudgetPill
          runId={activeRunId}
          onOverride={() => {
            if (canAdmin && activeRunId) openBudgetOverride(activeRunId)
          }}
        />
```

com `const canAdmin = useAuthStore((s) => s.can('admin'))` no App (substituir o selector `can('runner')` do RunsWorkspace se o lint reclamar de chamadas no selector — usar `const can = useAuthStore((s) => s.can)` uma vez e derivar `can('admin')`/`can('runner')`).

- [ ] **Step 4: Rodar testes p/ passar**

Run: `npx vitest run src/features/runs src/features/pipelines && npm run lint && npm run build`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/features/runs/NewRunForm.tsx src/features/runs/RunsWorkspace.tsx src/features/pipelines/PipelinesPanel.tsx src/app/App.tsx
git commit -m "feat(ui): ações gateadas por role (runs, pipelines, budget)"
```

---

### Task 7: E2E Playwright — login + RBAC na UI

**Files:**
- Modify: `web/loopforge-ade/frontend/src/app/App.tsx` (estender `__lfTest` hook, :112-120)
- Create: `web/loopforge-ade/frontend/tests/rbac.spec.ts`
- Modify: `web/loopforge-ade/frontend/tests/helpers.ts` (se necessário)

**Interfaces:**
- Consumes: `window.__lfTest` (App.tsx:114-119), `useAuthStore.setPrincipal` (Task 3)
- Produces: e2e provando: viewer não vê mcp/settings no rail nem NewRunForm; admin vê tudo

- [ ] **Step 1: Implementar hook de auth no __lfTest**

`App.tsx` (:112-120) — estender:

```tsx
    const hook = {
      dispatchWsEvent,
      runs: () => useRunsStore.getState().runs,
      activeRunId: () => useRunsStore.getState().activeRunId,
      setPrincipal: (p: { name: string; roles: string[] } | null) =>
        useAuthStore.getState().setPrincipal(p),
    }
```

- [ ] **Step 2: Escrever o spec**

`tests/rbac.spec.ts` (padrão dos specs existentes em `tests/`; usar `page.evaluate` p/ o hook):

```ts
import { expect, test } from '@playwright/test'

async function setPrincipal(page: import('@playwright/test').Page, roles: string[]) {
  await page.evaluate((r) => {
    ;(window as unknown as { __lfTest: { setPrincipal(p: { name: string; roles: string[] } | null): void } }).__lfTest.setPrincipal({ name: 'e2e-user', roles: r })
  }, roles)
}

test('viewer: sem mcp/settings no rail e sem NewRunForm', async ({ page }) => {
  await page.goto('/')
  await setPrincipal(page, ['viewer'])
  await page.getByLabel('Activity').waitFor()
  await expect(page.getByLabel('MCP playground')).toHaveCount(0)
  await expect(page.getByLabel('Settings')).toHaveCount(0)
  await expect(page.getByLabel('Idea')).toHaveCount(0)
})

test('admin: mcp/settings visíveis', async ({ page }) => {
  await page.goto('/')
  await setPrincipal(page, ['admin'])
  await expect(page.getByLabel('MCP playground')).toHaveCount(1)
  await expect(page.getByLabel('Settings')).toHaveCount(1)
})
```

Ajustar labels aos presentes na árvore de acessibilidade real (rodar `npm run dev` + `playwright` p/ conferir; `aria-label` do rail vem de `VIEWS_META` — `MCP playground`/`Settings`; NewRunForm textarea tem `aria-label="Idea"`).

- [ ] **Step 3: Rodar p/ passar**

Run: `npm run test:e2e -- --project=chromium tests/rbac.spec.ts`
Expected: PASS (backend não é necessário — o hook `setPrincipal` dispensa login real; se o spec depender de app montada sem backend, usar o padrão `dismissApiKeyGate` dos specs existentes no beforeEach)

- [ ] **Step 4: Suíte completa**

Run: `npm run test && npm run lint && npm run build && npm run test:e2e`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/app/App.tsx tests/rbac.spec.ts
git commit -m "test(e2e): RBAC — viewer vs admin na UI"
```

---

### Task 8: Validação final da fase

- [ ] **Step 1: Engine**

Run (em `agentes/LoopForge`): `ruff check --select E,F,W,I,N,UP,SIM src/lf tests && mypy src/lf && pytest --cov=src/lf --cov-fail-under=75 tests/`
Expected: PASS

- [ ] **Step 2: SPA**

Run (em `web/loopforge-ade/frontend`): `npm run lint && npm run test && npm run build && npm run test:e2e`
Expected: PASS

- [ ] **Step 3: Sync dist + smoke manual**

Run: `make sync-dist` (em `web/loopforge-ade`; copia dist p/ engine) → `lf serve` com `ade.yaml` contendo keys com roles diferentes → login com key runner (vê runs, sem settings/mcp) e key viewer (read-only).
