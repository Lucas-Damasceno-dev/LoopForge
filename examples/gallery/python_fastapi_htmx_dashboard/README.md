# 🐍 Exemplo 3: Python FastAPI + HTMX Financial Dashboard (Gerado pelo LoopForge)

Este projeto foi **100% gerado autonomamente** pela pipeline de agentes do LoopForge a partir do prompt:
> `"Dashboard financeiro em Python FastAPI com HTMX e SQLite"`

## 📌 Estrutura do Projeto
- `pyproject.toml`: Manifesto de dependências e configuração do pytest
- `main.py`: Servidor Web FastAPI com endpoints REST e renderização dinâmica HTMX
- `tests/test_main.py`: Suíte de testes unitários e de integração HTTP
- `lessons.md`: Relatório executivo da pipeline do LoopForge

## ⚙️ Como Executar e Testar
```bash
pytest
uvicorn main:app --reload
```
